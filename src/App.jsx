import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="starter-card">
        <p className="eyebrow">Fresh start</p>
        <h1>Ambuj Vashistha</h1>
        <p>
          Root-level Vite + React app is ready. Next we can build the notebook
          portfolio here without touching the old nested project.
        </p>
      </section>
    </main>
  )
}

export default App
